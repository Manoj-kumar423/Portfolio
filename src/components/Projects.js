import React from 'react';

const Projects = () => {
  return (
    <section id="projects" style={{ padding: '50px' }}>
      <h2>Projects</h2>
      <ul>
        <li>✅ Jenkins CI/CD pipeline for Java app on Tomcat</li>
        <li>✅ Dockerized Flask app with GitHub Actions & DockerHub</li>
        <li>✅ Kubernetes cluster deployment on AWS EC2 with YAML</li>
      </ul>
    </section>
  );
};

export default Projects;
